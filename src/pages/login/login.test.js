/* eslint-disable no-undef */
import { expect } from "chai";

/**
 * This file only satisfies compiler, doesn't do anything.
 * Add login tests here.
 */
it("Hello World Test", () => {
  expect(true).to.be.true;
});
